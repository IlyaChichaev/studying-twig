{#<div class="card" style="width: 16rem;">#}
{#    <img src="{{ image.src }}" class="card-img-top" alt="{{ image.alt }}" title="{{ image.title }}">#}
{#    <div class="card-body">#}
{#        <h5 class="card-title">{{ title }}</h5>#}
{#        <p class="card-text">{{ text }}</p>#}
{#        <button type="button" class="btn btn-primary">Go somewhere</button>#}
{#    </div>#}
{#</div>#}
<div class="flex-row">
    <div class="flex-item size-1">{{ ID }}</div>
    <div class="flex-item size-1">{{ Code }}</div>
    <div class="flex-item size-2">{{ Name }}</div>
    <div class="flex-item size-1">{{ Nominal }}</div>
    <div class="flex-item size-1">{{ Rate }}</div>
    <div class="flex-item size-2">{{ Date }}</div>
</div>